# Map UI Restore Report

Generated by `The Hero/UI/Restore Map UI`.

## Diagnosis
- Map scene contained duplicate/deprecated UI traces (`Deprecated_Canvas`) alongside `MapCanvas`, so UI could be hidden, duplicated, or disconnected after asset/map repair commands.
- The restore command normalizes the main Map UI to a root `MapCanvas` in Screen Space Overlay and disables deprecated/world-space UI leftovers.
- Active canvases after restore: 1. Deprecated canvases found: 1.

## Restored UI
- `TopHUD` with `GoldText`, `WoodText`, `StoneText`, `ManaText`.
- `SaveButton`, `LoadButton`, `EndTurnButton`, `MenuButton`.
- Small bottom-left `CastleButton` sized 140x48.
- `MessagePanel` with `MessageText`.
- `PauseOverlay` with Continue, Save, Load, Main Menu buttons.

## Runtime Wiring
- `THMapUIRuntime` is attached to `MapCanvas` and refreshes resource text from `THMapController`/`THManager` with fallback values 300/10/5/0.
- Top buttons call existing `THMapController` methods when available; otherwise they use safe fallback behavior.
- `MenuButton` and Esc open the restored `PauseOverlay`.
- `CastleButton` loads `Base`.

## Builder Protection
- Active map builders now preserve Canvas/EventSystem/UI. The old clean-map builder Canvas-child wipe is replaced with a preserve-UI comment.
- Validation scans active `TheHero*Map*.cs` editor scripts for Canvas deletion patterns.

## Validation
- PASS: Map UI validation succeeded.

## Manual Checks
- Play: MainMenu -> New Game -> Map.
- Confirm TopHUD, Save/Load/End Turn/Menu, small `ЗАМОК`, MessagePanel, and PauseOverlay are visible/usable.
- Confirm End Turn updates day/resources, Menu/Esc opens pause, Continue closes it, and Castle opens Base.
- Confirm no red Console errors.
